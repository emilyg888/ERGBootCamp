project_name: ERGBootCamp
database_path: db/rowing.duckdb

# Athlete profile
athlete:
  name: Em Gao
  height_cm: 160
  goal: "First indoor rowing competition"
  competition_date: "2026-11-11"
  training_start: "2026-02-01"

# LM Studio (OpenAI-compatible local inference)
lmstudio:
  base_url: "http://localhost:1234/v1"
  model: "qwen2.5-14b-instruct"
  max_tokens: 600
  temperature: 0.7

# Concept2 API
concept2:
  api_url: "https://log.concept2.com/api/users/me/results"
  replay_days: 3

# Discord webhook
discord:
  webhook_url: ""  # set via DISCORD_WEBHOOK_URL in .env

# Coaching context memory
coaching:
  context_window: 10
  recovery_split_buffer_sec: 12
  briefs_dir: "coaching/briefs/daily"
